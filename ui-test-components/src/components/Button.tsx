import React from 'react';


const AppButton = ( ) => {
    return (
        <button onClick={() => console.log("Button clicked")}>
            Button
        </button>
        
    );
};

export default AppButton;